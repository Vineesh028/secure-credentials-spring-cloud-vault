spring:
  application:
    name: javainuseapp
  cloud:
    vault:
      host: localhost
      port: 8200
      scheme: http
      token: hvs.wQKTErSSWGXUajmhQRGJvwnU
      generic:
        backend: secretengine1
        default-context: credentials